name: Portfolio Deployment

on:
  push:
    branches:
    - main  
  job:
    build:  

  runs-on: ubuntu-latest

  steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: print message
      run: echo "Deploying to AWS S3 bucket"

    - name: list files
      run: ls -la